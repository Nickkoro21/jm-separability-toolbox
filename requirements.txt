# ── Core UI ────────────────────────────────────────────────────────────
# Gradio >=5.20 includes gradio_client >=1.8 which fixes a TypeError
# in get_api_info() when a JSON schema contains a boolean value
# ("argument of type 'bool' is not iterable").
gradio>=5.20,<6.0
# huggingface_hub 1.0+ dropped HfFolder, which older Gradio versions import
# at startup. We both bump Gradio AND cap huggingface_hub as a safety net
# so an aggressive pip resolver cannot reintroduce the break.
huggingface_hub>=0.25,<1.0

# ── Numerical computing ─────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11

# ── Visualization ───────────────────────────────────────────────────────
plotly>=5.18
matplotlib>=3.7         # used for the class colour palette (tab10/tab20)
kaleido>=0.2.1          # static image export for Plotly figures
