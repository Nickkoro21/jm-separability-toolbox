# ── Core UI ────────────────────────────────────────────────────────────
# Pinned to versions verified in local development. Earlier Gradio
# versions hit a chain of issues on HF Space:
#   - 5.0   : ImportError HfFolder (huggingface_hub >=1.0 dropped it)
#   - 5.10  : same import broke when resolver pulled new hub
#   - 5.20  : gradio_client TypeError in /api_info ("bool not iterable")
#            AND dropdown change events stopped firing on first selection
# Gradio 5.50 has all of these fixed AND works with huggingface_hub >=1.0.
gradio>=5.50,<6.0
gradio_client>=1.14
huggingface_hub>=1.0,<2.0

# ── Numerical computing ─────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11

# ── Visualization ───────────────────────────────────────────────────────
plotly>=5.18
matplotlib>=3.7         # used for the class colour palette (tab10/tab20)
kaleido>=0.2.1          # static image export for Plotly figures
